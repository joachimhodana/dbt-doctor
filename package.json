{
  "name": "dbt-doctor",
  "private": true,
  "homepage": "https://github.com/joachimhodana/dbt-doctor#readme",
  "bugs": {
    "url": "https://github.com/joachimhodana/dbt-doctor/issues"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/joachimhodana/dbt-doctor.git"
  },
  "type": "module",
  "scripts": {
    "dev": "turbo run dev --filter=dbt-doctor",
    "build": "turbo run build",
    "test": "turbo run test --filter=dbt-doctor",
    "typecheck": "turbo run typecheck",
    "lint": "vp lint",
    "lint:fix": "vp lint --fix",
    "format": "vp fmt",
    "format:check": "vp fmt --check",
    "check": "vp check",
    "changeset": "changeset",
    "version": "changeset version",
    "release": "pnpm build && changeset publish",
    "leaderboard:update": "node --experimental-strip-types --no-warnings scripts/update-leaderboard.ts"
  },
  "devDependencies": {
    "@changesets/changelog-github": "^0.7.0",
    "@changesets/cli": "^2.31.0",
    "@types/node": "^25.6.0",
    "@voidzero-dev/vite-plus-core": "^0.1.15",
    "turbo": "^2.9.7",
    "typescript": "^6.0.3",
    "vite-plus": "^0.1.15"
  },
  "engines": {
    "node": ">=22",
    "pnpm": ">=8"
  },
  "packageManager": "pnpm@10.29.1",
  "pnpm": {
    "onlyBuiltDependencies": [
      "@parcel/watcher",
      "esbuild",
      "unrs-resolver"
    ]
  }
}
